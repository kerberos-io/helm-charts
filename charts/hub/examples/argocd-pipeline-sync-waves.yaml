# Opt-in ordering within one Argo CD Application; this does not enable services
# or upgrade their images. Select compatible releases in your environment values.
kerberospipeline:
  event:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "10"
  notify:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "20"
  throttler:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "30"
  analysis:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "40"
  thumbnail:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "50"
  sprite:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "50"
  dominantColor:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "50"
  counting:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "50"
  sequence:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "60"
  monitor:
    deploymentAnnotations:
      argocd.argoproj.io/sync-wave: "70"
